#ifndef QCOLOR_MAP_H
#define QCOLOR_MAP_H

#include <QColor>

/**
 * Mapping of physical values into a colors, it has a prefix q cause it is actively using qt inside
 */

namespace qcolor_map {
    template<class Float>
    QColor jet(Float val, Float min, Float max)
    {

    }
}

#endif // QCOLOR_MAP_H
